<h3>Categories Indax</h3>

<ul>
<% @categories.each do |category| %>
  <li>
 	  <%= category.name %>
    <%= link_to 'Edit', :controller => 'admin/categories', :action => 'edit', :id => category %>
    <%= link_to 'Delete', admin_category_path(category),method: :delete, data:{ confirm:"Are you sure?"}, :id => category %>
  </li>
<% end %>
</ul>
<%= link_to 'New Category', :controller => 'admin/categories', :action => 'new' %>
